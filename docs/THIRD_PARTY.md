# Third-party notices

TokenScope's original code and copy use the MIT License. Dependencies retain their own licenses.

The deployed site bundles DM Sans and IBM Plex Mono from the Fontsource packages. Both font families use the **SIL Open Font License 1.1**. Their original license texts are included in `public/licenses/` and therefore also in the built site and static release.

Runtime dependencies include React and React DOM (MIT). Build and test dependencies are recorded in `package-lock.json`; their license files are distributed through their respective npm packages.

The interface uses original SVG geometry and synthetic numerical fixtures. It does not redistribute papers, course videos, textbooks, or private study data. Academic references are linked rather than copied.
